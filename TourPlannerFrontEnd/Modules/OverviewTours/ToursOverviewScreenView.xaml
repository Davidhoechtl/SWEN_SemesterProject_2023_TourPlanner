<UserControl x:Class="TourPlannerFrontEnd.Modules.OverviewTours.ToursOverviewScreenView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TourPlannerFrontEnd.Modules.OverviewTours"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
  
        <Label x:Name="DisplayName" FontSize="40"/>
        <ContentControl x:Name="CreateTourViewModel" Grid.Row="1"/>

        <Button x:Name="CreateTour" Content="Create Tour" Grid.Row="2"/>
        <DataGrid x:Name="Tours" SelectedItem="SelectedTour" Grid.Row="3" IsReadOnly="True" SelectionMode="Single" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Path=Name}"/>
                <DataGridTextColumn Header="StartDate" Binding="{Binding Path=StartDate}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <ContentControl x:Name="SelectedTour" Grid.Column="1" Grid.Row="1" Grid.RowSpan="2"/>
    </Grid>
</UserControl>
